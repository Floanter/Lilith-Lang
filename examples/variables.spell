function main(argc: number, argv: string): void
    let age: number = 10;
    const name: string = "Bob";
    let pass: boolean = true;

    # Variable assigment.
    age += 1;
    age += 1 + age;
    age++;
    age--;

    # Pointer variable.
    let namePointer: string* = &name;

    # Array variable.
    let myNumbers: number[5] = [1, 2, 3, 4, 5];

    let otherNumbers: number[2];

    otherNumbers[0] = 1;
    otherNumbers[1] = 2;