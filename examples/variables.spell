void main ->
    int age = 20;

    age += 1 + 2;

    age--;

    bool pass = true;

    number sum = age + 1;

    shared float version = 2.0; 

    shared const char letter = "s";

    array<number> myNumbers = [2, 4, 6, 8]; 

    myNumbers[4] *= 8;

    array<number> otherNumbers[10];

    array<number> twoDArray[5][5];

    twoDArray[1][2] = 2;

    number* ptr = &age;
end